{
  "name": "tasktree",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "mkdir -p lib/ && curl 'https://github.com/finnhvman/matter/releases/download/0.2.2/matter.css' -L -o lib/matter.css",
    "test": "jest"
  },
  "dependencies": {
    "http-vue-loader": "^1.4.0",
    "vue": "^2.6.4"
  },
  "devDependencies": {
    "jest": "^24.7.1",
    "unexpected": "^11.4.1"
  }
}
